# AuntiesRecipe-Project
This app was built solo to demonstrate my .NET skills end-to-end. It’s a Blazor Web App on .NET 9 using Clean Architecture (Domain/Application/Infrastructure/Web), EF Core with SQLite, and ASP.NET Core Identity for authentication/roles, plus a full flow for menu → cart → checkout → order confirmation and admin management (content + menu + orders).
